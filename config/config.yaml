# GodHead Nexus Configuration (Updated for Stellar)
pi_coin:
  symbol: "PI"
  total_supply: 100000000000  # 100 billion PI
  fixed_value_usd: 314159  # $314,159 per PI (in cents)
  valid_sources: ["mining", "rewards", "p2p"]
stellar:
  network: "testnet"  # Change to "mainnet" for live
  contract_id: ""  # Will be set after deployment
  rpc_url: "https://soroban-testnet.stellar.org"
ai:
  autonomous_interval: 60
  hyper_ai_threshold: 0.95
  model_path: "ai/models/source_validator.pkl"
